name: broadcast_receiver
description: "A Flutter mobile application demonstrating broadcast receivers, media playback, and gestures."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  battery_plus: ^7.0.0
  video_player: ^2.8.0
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
